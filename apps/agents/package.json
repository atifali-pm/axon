{
  "name": "@axon/agents",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.main:app --host 0.0.0.0 --port ${AGENTS_PORT:-8000} --reload",
    "build": "echo 'no build step (python)'",
    "typecheck": "echo 'no typecheck (python mypy wired later)'",
    "lint": "echo 'lint via ruff (run manually for now)'",
    "clean": "find . -type d -name __pycache__ -prune -exec rm -rf {} +"
  }
}
